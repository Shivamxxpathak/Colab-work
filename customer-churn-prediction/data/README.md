# Dataset

Place the IBM Telco Customer Churn dataset in this directory as:

`WA_Fn-UseC_-Telco-Customer-Churn.csv`

Expected target column: `Churn` with values `Yes` / `No`.

The public dataset contains 7,043 customers and 21 columns. The raw CSV is ignored by Git to keep the repository lightweight.
